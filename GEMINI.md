# Gemini project instructions

Read and follow `docs/AI_DEVELOPMENT.md` and `AGENTS.md` before modifying this repository.

Preserve the protected super administrator and idempotent initialization invariants. Do not expose `.env` secrets. Use installed package versions as the API source of truth and run the relevant verification commands before reporting completion.
