# Aider conventions

Follow `docs/AI_DEVELOPMENT.md` and `AGENTS.md` for all changes.

Do not weaken the protected super administrator or idempotent initialization behavior. Never expose `.env` secrets. Use APIs compatible with the installed lock-file versions and run the applicable verification commands before completion.
